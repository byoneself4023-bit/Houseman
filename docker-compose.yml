services:
  db:
    image: postgres:16-alpine
    container_name: houseman-db
    environment:
      POSTGRES_DB: houseman
      POSTGRES_USER: houseman
      POSTGRES_PASSWORD: houseman
    ports:
      - "5434:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U houseman"]
      interval: 5s
      timeout: 5s
      retries: 5

  server:
    build:
      context: ./houseman-server
    container_name: houseman-server
    depends_on:
      db:
        condition: service_healthy
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://db:5432/houseman
      SPRING_DATASOURCE_USERNAME: houseman
      SPRING_DATASOURCE_PASSWORD: houseman
      SPRING_PROFILES_ACTIVE: prod
    ports:
      - "8080:8080"

  client:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: houseman-client
    depends_on:
      - server
    ports:
      - "3000:80"

volumes:
  pgdata:
